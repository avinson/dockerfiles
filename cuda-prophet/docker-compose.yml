version: "3"
volumes:
  work:
    driver: local

services:
  notebook:
    build: .
    runtime: nvidia
    volumes:
      - work:/home/jovyan/work:cached
    ports:
      - 8888:8888
    user: root
    environment:
      GRANT_SUDO: 'yes'
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]